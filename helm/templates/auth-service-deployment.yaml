{{- range .Values.namespaces }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ $.Values.services.auth.name }}
  namespace: {{ . }}
spec:
  replicas: {{ $.Values.replicaCount }}
  strategy:
{{ toYaml $.Values.deploymentStrategy | indent 4 }}
  selector:
    matchLabels:
      app: {{ $.Values.services.auth.name }}
  template:
    metadata:
      labels:
        app: {{ $.Values.services.auth.name }}
    spec:
      containers:
      - name: {{ $.Values.services.auth.name }}
        image: {{ $.Values.image.registry }}/{{ $.Values.services.auth.image }}:{{ $.Values.services.auth.tag }}
        imagePullPolicy: {{ $.Values.image.pullPolicy }}
        envFrom:
        - configMapRef:
            name: {{ $.Values.configMap.name }}
        - secretRef:
            name: {{ $.Values.secret.name }}
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: {{ $.Values.secret.name }}
              key: DATABASE_URL
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: {{ $.Values.secret.name }}
              key: JWT_SECRET
        ports:
        - containerPort: {{ $.Values.services.auth.port }}
        resources:
{{ toYaml $.Values.services.auth.resources | indent 10 }}
        volumeMounts:
        - name: secrets-store-inline
          mountPath: /mnt/secrets-store
          readOnly: true
      volumes:
      - name: secrets-store-inline
        csi:
          driver: secrets-store.csi.k8s.io
          readOnly: true
          volumeAttributes:
            secretProviderClass: {{ $.Values.secretProviderClass.name }}
{{- end }}
