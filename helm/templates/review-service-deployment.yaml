{{- range .Values.namespaces }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ $.Values.services.review.name }}
  namespace: {{ . }}
spec:
  replicas: {{ $.Values.replicaCount }}
  strategy:
{{ toYaml $.Values.deploymentStrategy | indent 4 }}
  selector:
    matchLabels:
      app: {{ $.Values.services.review.name }}
  template:
    metadata:
      labels:
        app: {{ $.Values.services.review.name }}
    spec:
      containers:
      - name: {{ $.Values.services.review.name }}
        image: {{ $.Values.image.registry }}/{{ $.Values.services.review.image }}:{{ $.Values.services.review.tag }}
        imagePullPolicy: {{ $.Values.image.pullPolicy }}
        envFrom:
        - configMapRef:
            name: {{ $.Values.configMap.name }}
        - secretRef:
            name: {{ $.Values.secret.name }}
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: {{ $.Values.secret.name }}
              key: DATABASE_URL
        ports:
        - containerPort: {{ $.Values.services.review.port }}
        resources:
{{ toYaml $.Values.services.review.resources | indent 10 }}
        volumeMounts:
        - name: secrets-store-inline
          mountPath: /mnt/secrets-store
          readOnly: true
      volumes:
      - name: secrets-store-inline
        csi:
          driver: secrets-store.csi.k8s.io
          readOnly: true
          volumeAttributes:
            secretProviderClass: {{ $.Values.secretProviderClass.name }}
{{- end }}
