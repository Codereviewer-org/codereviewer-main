apiVersion: apps/v1
kind: Deployment
metadata:
  name: review-service
  namespace: codereviewer

spec:
  replicas: 1

  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 0
      maxUnavailable: 1

  selector:
    matchLabels:
      app: review-service

  template:
    metadata:
      labels:
        app: review-service

    spec:
      containers:
      - name: review-service
        image: thanusri22/review-service:v2
        imagePullPolicy: Always

        envFrom:
        - configMapRef:
            name: coderaptor-config

        - secretRef:
            name: kv-secret

        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: kv-secret
              key: DATABASE_URL

        ports:
        - containerPort: 8004

        resources:
          requests:
            cpu: 10m
            memory: 64Mi

        volumeMounts:
        - name: secrets-store-inline
          mountPath: /mnt/secrets-store
          readOnly: true

      volumes:
      - name: secrets-store-inline
        csi:
          driver: secrets-store.csi.k8s.io
          readOnly: true
          volumeAttributes:
            secretProviderClass: kv-secrets
---
apiVersion: v1
kind: Service
metadata:
  name: review-service
  namespace: codereviewer
spec:
  selector:
    app: review-service
  ports:
  - port: 8004
    targetPort: 8004
  type: ClusterIP
